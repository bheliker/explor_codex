[project]
name = "explor-codex"
version = "0.1.0"
description = "A plain Flask-ready app template managed with uv."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1,<4",
]

[tool.uv]
package = false
